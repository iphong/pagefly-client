import { Container } from 'unstated-x'

const elements: { [key: string]: any } =  {
	'Section': require('../shared/elements/Section').default,
	'Button': require('../shared/elements/Button').default
}

export default elements