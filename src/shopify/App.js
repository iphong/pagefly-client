import React, { Component } from 'react';

class App extends Component {

  componentDidMount() {
    // fetch('/shopify/test', {credentials: 'include'}).then(res => res.json()).then(console.log)
  }

  render() {
    return (
      <div className="App">
        App is
      </div>
    );
  }
}

export default App;
